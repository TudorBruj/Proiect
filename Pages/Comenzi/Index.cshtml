@page
@model Proiect.Pages.Comenzi.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Comenzi:</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Comanda[0].Client)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comanda[0].DataComenzii)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comanda[0].TotalPlatit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Comanda) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.Client.Nume) @Html.DisplayFor(modelItem => item.Client.Prenume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataComenzii)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalPlatit)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
